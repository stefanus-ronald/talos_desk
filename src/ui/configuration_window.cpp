#include "core/logger.h"

namespace talos {

// Placeholder for configuration window implementation
class ConfigurationWindow {
public:
    ConfigurationWindow() {
        Logger::instance().debug("ConfigurationWindow created");
    }
};

} // namespace talos