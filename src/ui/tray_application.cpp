#include "core/logger.h"

namespace talos {

// Placeholder for tray application implementation
class TrayApplication {
public:
    TrayApplication() {
        Logger::instance().debug("TrayApplication created");
    }
};

} // namespace talos