#include "core/logger.h"

namespace talos {

// Placeholder for codec manager implementation
class CodecManager {
public:
    CodecManager() {
        Logger::instance().debug("CodecManager created");
    }
};

} // namespace talos